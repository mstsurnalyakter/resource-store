/**
 * Create a function that takes two numbers as parameters and returns their sum.
 */

const sum = (num1,num2) =>{
    return num1 + num2;
}



console.log(`Sum = ${sum(3, 4)}`);


/**
 * Write a function that takes two numbers as parameters and returns their product.
 */

const multi = (num1, num2) => {
  return num1 * num2;
};

console.log(`Multiplicate ${multi(3, 4)}`);


/**
 * Implement a function that calculates the power of a number (e.g., power(2, 3) should return 8).
 */

const power = (num) =>{
    return Math.pow(num,3);
}

console.log(`Power = ${power(3)}`);