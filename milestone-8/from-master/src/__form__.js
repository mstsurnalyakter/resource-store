/**
 * 1. Use form tag and onSubmit handler with event (e) from e  e.target.[name of the input field].value and name to get value
 *
 * 2.controlled element: use individual field state form each field
 *
 * 3. controlled element: one object in a holding all the input field value
 *
 * 4. uncontrolled element:useRef to create a reference to the element and access value by using like: nameRef.current.value
 *
 * 5. use hook to handle states and submit and error. example react hook form
 *
 * 6. Reuseable component.
 */